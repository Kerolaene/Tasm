;?????? ? ?????????? ??? ????? ? ??????? ? ????????? ??????  ?????? ? ??? ????????? ????? ??????? ?????, ? ????? ??????? ???????? ? ?????????? ?1.
data segment
slov_1 db 'input first word =>','$'
slov_2 db 'input second word =>','$'
i db 'i', '$'
mes2 db 10,13,'$'
buf db 30
db ?
db 30 dup (?)
buf_2 db 30
db ?
db 30 dup (?)
data ends
stk segment stack
    db 256 dup('?')
stk ends
code segment
main:
assume cs:code,ds:data,ss:stk
mov ax,data
mov ds,ax
mov ah,9;input result
lea dx,slov_1
int 21h 
mov ah,10
lea dx,buf
int 21h

mov bl,buf+1
mov bh,0
mov buf[bx+2],'$'
mov si,1
mov dl,buf[si]

mov ah,9;input result
lea dx,mes2
int 21h
lea dx,slov_2
int 21h

mov ah,10
lea dx,buf_2
int 21h
mov bl,buf_2+1
mov bh,0
mov buf_2[bx+2],'$'

int 21h
mov ax,4c00h
int 21h
code ends
end main